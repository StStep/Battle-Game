using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GuiMoveObject : GuiSegObject
{
    public override void Init(GameObject par, Selector sel, GuiRender ren,
        ICommandRef cmdRef)
    {
        base.Init(par, sel, ren, cmdRef);
    }
}
